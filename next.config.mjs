/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/scs-ui",
    //output: "export", // build static
    compiler: {
        styledComponents: true
    },
};

export default nextConfig;
